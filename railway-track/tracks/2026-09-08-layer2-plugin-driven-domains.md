# Layer 2 plugin-driven domains

## 2026-09-08 10:50 IST

**User**

Implement the approved Layer 2 schema-4 plugin-driven design: three frozen inputs, complete window reading, domain design, original-source distribution, one paged reviewer, immutable facts and independent assignments, partial publication, bounded read-only retrieval and 200K/250K input policy. Keep Layers 3/4 and historical artifacts unchanged. Claude implements; Codex supervises and verifies after resolving task-scoped write access.

**Agent**

Superseded: Claude's native patch command remained permission-denied. No implementation files changed. The user explicitly authorized Codex to implement directly in the next checkpoint.

**Verification**

Verified Team authentication, a local empty-patch invocation, worker termination and unchanged pre-existing file hashes. Claude's permission preflight failed; no application tests or research calls ran. Global Claude settings are unchanged.

## 2026-09-08 11:04 IST

**User**

Forget Claude; Codex should implement the approved Layer 2 schema-4 plan directly.

**Agent**

Completed: Codex implemented Layer 2 schema 4 directly under the user's override. Three frozen inputs drive source understanding, dynamic domain design, original-source distribution and one paged review. Immutable facts, independent assignments, versioned publications and unresolved audits preserve evidence. Native read-only StateBackend tools, SQLite recovery, input accounting, CLI/PowerShell controls and the status-only notebook cell are implemented. No schema-4 handoff to unchanged Layers 3/4 is generated. Changes are local and uncommitted; the user must supply a requirements file before a real run.

**Verification**

114 offline tests passed, including native tool isolation, interrupted SQLite recovery without repeated reads, immediate out-of-order saving, plugin-dependent rosters, exact 701,133-token/14-window handling, Unicode preservation, input repacking and observational publication. Compilation, pip check, PowerShell parsing, notebook compilation/kernel checks, function docstrings, 350-line checks and git diff --check passed. Hash comparisons confirmed 89 protected files unchanged, including Layers 3/4, inputs and prior architecture documents; the Layer 3/4 notebook cells are identical. No live application model or research calls ran. Offline checks establish orchestration, not extraction completeness or research quality.
