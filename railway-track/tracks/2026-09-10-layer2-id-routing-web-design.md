# Layer 2 V3: stable domain routing and web-assisted design

## 2026-09-10 19:08 IST

**User**

Implement fresh schema 9 with ID-keyed JSON planning/distribution and Markdown metadata/domain views. Broaden the real-estate plugin and requirements, enable optional native web search only for the designer with explicit public-input confirmation, and preserve important fact qualifications. Replace visible unresolved.md with internal routing diagnostics. Keep the three stages, model/settings, historical runs and existing dirty work; no live model calls.

**Agent**

Completed on `plain_research_v3`. New runs use schema 9, stable domain IDs and permissive JSON-object planning/distribution. The direct designer alone has native, automatically selected web search; consent and medium search context are frozen, with complete request accounting and a 128K designer ceiling. Metadata stays unchanged and distribution still reads original source windows.

Broadened the authorized plugin/requirements and replaced heading routing with ID routing. Raw responses, duplicate members and unusable values remain preserved internally; README warnings link to routing diagnostics. Resume retains completed responses without repair calls, and publication keeps successful siblings and previous revisions. Updated notebook confirmation, CLI/PowerShell controls, guides and the fixed 15-case manual benchmark.

Preserved the pre-existing dirty schema-8 work, historical runs and Layer 3/4 behavior. Layer 2 production Python changed from 1,079 to 1,146 lines (+67); no new dependency, agent, reviewer or model-output grader was added. Changes remain uncommitted.

**Verification**

Completed 2026-09-10 19:46 IST:

- Full Docker offline suite: **137 tests passed in 89.711 seconds**, with socket connections blocked. Coverage includes native request serialization, consent, duplicate/unknown IDs, safe usage logging, interrupted writes, resume, source-order partial publication and historical rejection.
- Compilation, dependency checks, three notebook-cell compilation, PowerShell parsing, function docstrings, executable file-length checks and `git diff --check` passed.
- SHA-256 comparison of 44 protected paths found only the explicitly authorized requirements change; the other 43 remained identical. Layer 3/4 notebook cells and all saved notebook outputs remained unchanged.
- No live model/search call, research execution, historical-run recovery, container restart, migration or commit occurred. Native provider acceptance and real extraction quality remain unverified; the source-backed benchmark is for a later authorized comparison.
