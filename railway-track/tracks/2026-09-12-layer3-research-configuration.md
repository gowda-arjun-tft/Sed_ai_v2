# Layer 3 research purpose and execution configuration

## 2026-09-12 13:15 IST

**User**

Keep research purpose in the existing editable Markdown instruction. Add a frozen
three-field JSON call policy for new full/linked research capability-version-3 runs:
80/60/70 by default, or all null for unlimited application logical calls. Preserve
historical policies, accounting, memory, tools, sequential execution and outputs.
Expose API, CLI, PowerShell and notebook controls; verify offline without live calls.

**Agent**

Completed: New full and linked schema-9 runs freeze research capability version 3 and
the exact selected config bytes/hash plus resolved call limits. Default 80/60/70
behavior is retained; three nulls keep durable usage tracking without budget-driven
finalization, tool restrictions or exhaustion. Reused the existing CallBudget and
summarizer integration rather than adding a new harness or middleware stack.

Added API, CLI, PowerShell/Docker path and notebook controls. Logs, README and notebook
display unlimited allowance explicitly. Existing version-1/version-2 policies do not
load the new config. Prompts and the editable user research instruction are unchanged;
Deep Agents Builder guided reuse of native capabilities, and Prompt Framework confirmed
that report purpose already lives in the selected Markdown rather than permanent prompts.

Updated Layer 3/root/Docker guides, AGENTS.md, Railway Track vision/architecture and this
track. Changes are local and uncommitted. No live searches, uploads, model calls,
research restarts, container restarts or historical-run rewrites were performed.

**Verification**

- Docker full offline suite: 214 tests passed in 152.688 seconds.
- Focused config/budget/controls/document/handoff/memory suite: 28 tests passed in
  33.706 seconds. Tests include default and custom thresholds, invalid configs before
  run creation, exact BOM/CRLF snapshots read once, parent preservation, historical
  version-1/version-2 behavior, canceled/failed reservations and resume.
- Native fake-model graph exceeded 80 logical calls while retaining all research tools
  and performing summarization after call 80; completed Markdown was saved verbatim and
  reused. Fake search/document requests after call 80 shared the unlimited ledger.
- Existing alternate-objective and compaction tests passed; the frozen user instruction
  remains active without risk-heading completion gates or content repair.
- Compilation of ML/tests/docker and pip check passed; Deep Agents remains 0.7.7 using
  Linux /usr/local/bin/python. No dependency changes.
- Notebook JSON/all three code cells compile; only Layer 3 source changed, with saved
  outputs and other cells preserved. PowerShell parsing and interface checks passed.
- File-length and changed-production-function docstring checks passed; git diff --check
  passed. No changes to Layer 2/4, researcher prompts or existing inputs.
- Disposable checkpoint preflight passed on the dedicated Docker volume for both
  bounded and unlimited policies. Development/browser container IDs and start times
  remained unchanged.
- Offline checks establish configuration/accounting/recovery, not live research quality
  or provider credit availability. Unlimited mode can continue spending until independent
  completion, cancellation or operational/provider limits intervene.
