# Layer 2 readable publication and less repeated model text

## 2026-09-08 18:44 +05:30

**User**

Implement schema 5 with consolidated internal fact/assignment storage, readable domain outputs and complete unprocessed-output visibility; separately reduce repetitive review/assignment text. Preserve evidence, output freedom, current settings and historical runs; do not change Layers 3/4 or make live calls.

**Agent**

Completed: implemented fresh-run schema 5 with consolidated immutable fact storage, separate ownership, readable domain/plan views and full unprocessed-value visibility. Retained raw responses, input fingerprints, SQLite recovery and prior publication/view bytes. Separately revised three prompts and review inputs to reduce repeated text while retaining every supplied fact and paged evidence. Model settings (including the user's high default), the six stages and tool surfaces are preserved. Only the Layer 2 notebook comment changed; existing notebook outputs/settings and requirements edits were preserved. Layer 3/4 implementation and historical research artifacts remain unchanged. Documentation and Railway current truth updated; work remains local and uncommitted.

**Verification**

Storage-first checks: 21 focused tests and 6 new publication tests passed before prompt changes. Final full offline suite: 122 tests passed. A further 15 focused tests passed after strengthening internal usage-path/checkpoint coverage. Compilation, pip check, PowerShell parsing, notebook JSON/three cells/compute kernel, function docstrings, 350-line checks and git diff --check passed. Verified original-source 701,133-token fixture, arbitrary JSON/audit visibility, conflict/write recovery, no content retry, same-thread retrieval and exact native usage totals. The before/after runs inventory fingerprint (paths, sizes and modification timestamps) and requirements-file hash match; no Layer 3/4 code diff. No model, web-search or paid API calls. Offline checks do not establish extraction completeness or token/quality improvement; that requires a separately authorized real comparison.
