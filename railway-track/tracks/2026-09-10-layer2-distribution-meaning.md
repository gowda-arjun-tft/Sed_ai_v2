# Layer 2 complete meanings during distribution

## 2026-09-10 22:32 IST

**User**

Refine only the production distribution prompt to preserve complete research meanings, qualifiers and figure scope. Keep schema 9 and three stages; update the fixed benchmark to evaluate shared metadata plus domain content. Verify offline, preserving historical runs and all other production behavior. No paid comparison or automatic recovery.

**Agent**

Completed: Restructured only the production distribution prompt using Prompt Framework: complete rules and separate consequences, figure scope, comparable contradictions, meaning-first domain organization and two generic examples. Retained routing/output/overlap contracts; no additional artifacts, stages or validators. Updated the fixed benchmark with the existing schema-9 baseline and combined metadata/domain evaluation, preserving all B01–B15 cases. Added focused preservation/snapshot checks and updated the Layer 2 guide and architecture note. Changes are local and uncommitted.

**Verification**

All 141 offline unit tests passed in Docker (93.922s), with socket connections blocked. Includes exact Unicode/qualified-statement/scoped-figure raw storage and multi-domain publication, completed-response reuse, unchanged old snapshots and new prompt hashes; existing empty/unconventional-output, schema, tool-surface and recovery tests remain passing. Compilation, pip check, git diff --check and untracked-file whitespace checks passed; full suite includes notebook, docstring and file-length checks. SHA-256 inventory confirmed 221 pre-existing files unchanged, including both inspected real runs, other production code/prompts, notebook and inputs; the seven pre-existing changed files are exactly the authorized prompt/tests/docs, with a new track and final change-log entry.

Offline tokenizer measurement: distribution prompt 579 -> 822 tokens (+243), using the frozen baseline prompt whose hash matched the pre-edit source. Revised SHA-256: `7c91c6bfcf6a7ed46551db973af09b9f5302e690658fd25786c7f875e6aae5bc`. The prompt is longer to clarify measured failure modes; no output-token savings or extraction improvement has been demonstrated. No model call, research run, current-run recovery or paid experiment occurred. A four-distribution-window fixed-settings comparison remains separately authorized work, not an installed stage or completed quality evaluation.
