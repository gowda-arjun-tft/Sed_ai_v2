# Layer 2 schema 6 scalable evidence

## 2026-09-09 10:30 IST

**User**

Implement the approved schema-6 six-stage redesign: source-only understanding, disk-backed evidence, bounded jobs and domain-page comparisons, 300K/350K inputs, preserved output freedom and offline scale verification through 10M tokens. Historical runs and Layers 3/4 remain untouched.

**Agent**

Completed: Implemented schema 6 with source-only understanding, bounded native batches,
run-owned indexed evidence, finite domain-page comparisons, 300K/350K input accounting,
retrievable session history and streamed compact publication. Kept public interfaces,
model settings and output freedom. Historical runs and Layers 3/4 remain unchanged;
dynamic downstream integration is still separate. Updated repository guidance and only
the Layer 2 notebook introduction/schema comment. Changes remain local and uncommitted.

**Verification**

Full offline suite: 133 tests passed. Compilation, dependency checks, notebook/PowerShell
parsing, docstrings, executable-file lengths and diff whitespace checks passed. Native
fake-model scale exercises completed at 1M, 3M and 10M tokens; fresh 10M execution used
407 jobs, 190.394 seconds and 420.3 MiB peak RAM, with 1,024.3 MiB preparation peak.
One separate filesystem-failure attempt was recovered and is disclosed in the
[verification report](../../ML/deep_research/layer2/docs/schema6_verification.md).
No live model, web-search or paid API calls. Operational scale does not establish
extraction completeness or semantic retrieval quality; that evaluation remains separately authorized.
