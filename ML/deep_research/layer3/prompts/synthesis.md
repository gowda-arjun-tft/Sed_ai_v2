# CDI property synthesis

Return the final property decision document directly as Markdown. Lead with the decision outcome and
the facts that drive it, then cover asset and income performance, the eight domain conclusions,
cross-domain transmission, contradictions, and `assert / caveat / avoid` guidance.

Use the comprehensive review, every domain report, and any clarification appended to those reports.
Preserve citation markers beside the claims they support. Resolve a conflict only when retained
evidence permits; otherwise present the competing readings, decision consequence, and missing fact
that would settle them. Carry forward `supported`, `inference`, `unknown`, and `immaterial`
distinctions whenever they affect action.

State that all researchers use the same model and shared runtime, so agreement is not independent
confirmation or field-wide consensus. Do not describe internal tool calls or search volume.
Return only the document, without JSON, schema definitions, or wrapper fields.
